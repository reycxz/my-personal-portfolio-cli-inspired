---
import { FOOTER } from "../consts";
---

<footer
	class="safe-px safe-pb border-t p-2 sm:p-3 font-mono static md:fixed md:bottom-0 md:left-0 md:right-0 z-10"
	style={{
		backgroundColor: "var(--theme-background)",
		borderColor: "var(--theme-border)",
	}}
>
	<div
		class="max-w-screen-xl mx-auto flex flex-col gap-2 md:flex-row md:items-center md:justify-between text-xs"
	>
		{/* Mobile layout - stacked */}
		<div class="flex flex-col md:hidden items-center space-y-1 w-full text-center">
			<div
				class="flex items-center space-x-2"
				style={{ color: "var(--theme-muted)" }}
			>
				<div
					class="w-2 h-2 rounded-full animate-pulse"
					style={{ backgroundColor: "var(--theme-primary)" }}
				>
				</div>
				<!-- <span style={{ color: "var(--theme-primary)" }}
                    >{footer.statusMessage}</span
                > -->
				<span>•</span>
				{
					(
						// footer.madeWithLove.enabled && (
						<span>
							{FOOTER.madeWithLove.text} on {FOOTER.madeWithLove.location}
						</span>
						// )
					)
				}
			</div>
			{FOOTER.systemStatus.enabled && (
				<div style={{ color: "var(--theme-primary)" }}>
					{FOOTER.systemStatus.message}
				</div>
			)}
		</div>

		{/* Desktop layout - horizontal */}
		<div
			class="hidden md:flex items-center space-x-4"
			style={{ color: "var(--theme-muted)" }}
		>
			<span>Session: active</span>
			<span>•</span>
			<span>TTY: /dev/pts/0</span>
			<span>•</span>
			<span style={{ color: "var(--theme-primary)" }}
				>{FOOTER.statusMessage}</span
			>
		</div>

		{/* Center - Made with love (desktop only) */}
		{
			FOOTER.madeWithLove.enabled && (
				<div
					class="hidden md:flex items-center space-x-1"
					style={{ color: "var(--theme-muted)" }}
				>
					<span>{FOOTER.madeWithLove.text}</span>
					<span>on {FOOTER.madeWithLove.location}</span>
				</div>
			)
		}

		<!-- {/* Right side - Status (desktop only) */} -->
		{
			FOOTER.systemStatus.enabled && (
				<div
					class="hidden md:flex items-center space-x-2"
					style={{ color: "var(--theme-muted)" }}
				>
					<div
						class="w-2 h-2 rounded-full animate-pulse"
						style={{ backgroundColor: "var(--theme-primary)" }}
					/>
					<span>{FOOTER.systemStatus.message}</span>
				</div>
			)
		}
	</div>
</footer>
