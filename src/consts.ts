// Place any global data in this file.
// You can import this data from anywhere in your site by using the `import` keyword.
import profileConfig from "../profile.config";

export const SITE_TITLE = profileConfig.profile.name + "'s Profile";
export const SITE_DESCRIPTION = profileConfig.profile.description;
export const NAVIGATION = profileConfig.navigation;
export const FOOTER = profileConfig.footer;
export const terminalConfig = profileConfig.terminal;